<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abbasnaqdi.sample"> <!-- Ensure this matches -->

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ico_app"
        android:label="@string/str_app_name"
        android:roundIcon="@mipmap/ico_app_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" <!-- This theme might need to be updated for M3 -->
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".MainActivity" <!-- Changed from .MainView -->
            android:exported="true"
            android:theme="@style/AppTheme"> <!-- Ensure theme is M3 compatible or remove if set by Composable -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>